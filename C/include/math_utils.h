#ifndef MATH_UTILS_H
#define MATH_UTILS_H

// Fonction pour calculer la somme des nombres pairs
int sum_even_numbers(int n);

// Fonction pour calculer la factorielle
long long factorial(int n);

// Fonction pour vérifier si un nombre est premier
int is_prime(int n);

// Fonction pour calculer le PGCD
int gcd(int a, int b);

// Fonction utilitaire pour afficher un tableau
void print_array(int arr[], int size);

#endif // MATH_UTILS_H
